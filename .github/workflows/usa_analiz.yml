name: USA Portfolio Analysis

on:
  schedule:
    - cron: '0 20 * * 1-5' # Hafta içi her gün TSİ 23:00 (Borsa Kapanışı)
  workflow_dispatch:      # Manuel çalıştırma için

jobs:
  run-portfolio-manager:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run USA Quant Engine
        env:
          PORTFOLIO_TYPE: 'ABD'
          PORTFOLIO_CAPITAL: '10000'
          # Güncellenmiş portföy listesi:
          STOCK_LIST: 'VOO,SCHD,QQQM,SPUS,VXUS,O,WPC,SMH,AIS,NASA,EUV,CHAT,NVDA,AVGO,GOOG,CAT,LENZ,ASPI'
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          WOLFRAM_ALPHA_APPID: ${{ secrets.WOLFRAM_ALPHA_APPID }}
          USE_AI: 'true'
        run: python main.py
